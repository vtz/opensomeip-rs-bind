[package]
name = "opensomeip"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Safe Rust bindings for OpenSOME/IP"

[features]
default = ["std"]
std = []

[dependencies]
opensomeip-sys = { path = "../opensomeip-sys" }
