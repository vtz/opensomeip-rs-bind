[package]
name = "opensomeip-sys"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Raw FFI bindings to opensomeip C API"

[build-dependencies]
# No bindgen — hand-written or vendored extern C
